/*
	Add your SAP Business ByDesign scripting language implementation for:
		Business Object: BOPyGExecution
		Node: Root
		Validation: OnSave 

		Script file signature
		----------------------------
		Returns: Boolean
		
	Note: 
	  - To access the elements of the business object node, 
	    use path expressions, for example, this.<element name>. 
	  - To use code completion, press CTRL+J. 
	  - The solution uses this script before an instance of the business object is saved to the data base.
		It can have a return value of either true or false, which determines whether the instance of the business object is saved or not.
		All fields in this business object are read-only and cannot be changed.
*/

import ABSL;

var vIsConsistent = true;
//if (this.TypeReport!= CLReportPYGCode.CO_PYG5){
	//if(this.ID.IsInitial()){
		//vIsConsistent = false;
		//raise msj.Create("E","Falto ingresar ID de estructura ");
	//}
//}
//else {
	//if(this.IDEquity.IsInitial()){
		//vIsConsistent = false;
		//raise msj.Create("E","Falto ingresar ID de estructura ");
	//}
//}
//
//
//if(this.ReportDate.IsInitial()){
	//vIsConsistent = false;
	//raise msj.Create("E","Falto ingresar fecha de reporte");
//}
//
//if(this.DateFrom.IsInitial()){
	//vIsConsistent = false;
	//raise msj.Create("E","Falto ingresar Periodo");
//}
//
////if(this.DateTo.IsInitial()){
	////vIsConsistent = false;
	////raise msj.Create("E","Falto ingresar Periodo final");
////}
//
////if(this.Currency.IsInitial()){
	////vIsConsistent = false;
	////raise msj.Create("E","Falto ingresar moneda");
////}


return vIsConsistent;